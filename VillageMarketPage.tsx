/**
 * Legacy entry — the homepage uses the same UI from `src/pages/VillageMarketPage.tsx`
 * via `src/routes/index.tsx`. Import from there for a single source of truth.
 */
export { default } from "./src/pages/VillageMarketPage";
